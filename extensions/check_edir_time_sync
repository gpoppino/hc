
check_edir_time_sync()
{
    NDSREPAIR=$(which ndsrepair 2>/dev/null)

    [ -z ${NDSREPAIR} ] && return 1

    total_errors=$(${NDSREPAIR} -T | grep -w No | wc -l)
    RETVAL=$?

    [ $RETVAL -ne 0 ] && return 1

    [ $total_errors -ne 0 ] && return 1
    return 0
}

